{
  "name": "ncc",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "format": "prettier --write '**/*.{js,ts}'",
    "lint": "eslint --ext .ts"
  }
}
